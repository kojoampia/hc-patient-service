# Build, test and scan hc-patient-service.
#
# There was no CI in this repository at all before 2026-08-05, which is how a service with no
# object-level authorization, a committed JWT signing key and a forgeable audit trail stayed that way
# — the ArchUnit layer test, the JWT tests and the whole integration suite ran only when somebody
# remembered to run them locally.
#
# Scanning starts NON-BLOCKING (continue-on-error) on purpose. The dependency and secret scans will
# light up on day one — the committed keys are in git history and cannot be removed from it — and a
# job that is red on arrival is a job everybody learns to ignore. Triage the findings, then delete the
# continue-on-error lines so this actually gates. That second step is the point; without it this file
# is decoration.
name: Build and scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The pom targets 25 via maven.compiler.release and the Enforcer accepts [17,26). Temurin 25 is
      # what the production image runs (docker/api.Dockerfile), so it is what this builds on.
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: temurin
          cache: maven

      # The *IT suite starts MongoDB and Kafka through Testcontainers, so a working Docker daemon is
      # the only service dependency — `npm run services:up` is deliberately NOT needed here.
      - name: Build and test
        run: ./mvnw --batch-mode --no-transfer-progress verify

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            target/surefire-reports/
            target/failsafe-reports/
            target/site/jacoco-merged/
          retention-days: 7

  scan:
    name: Dependency and secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Secret scanning needs history, not just the tip: the point is to find what was committed
          # and later deleted, which is exactly the shape of this repo's JWT key.
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: temurin
          cache: maven

      # Known-vulnerable Java dependencies. Spring Boot 4.0.6 and Spring Cloud 2025.1.1 are current,
      # but nothing was checking, and "current today" has a short shelf life.
      #
      # Non-blocking until it has been run once and triaged. It could not be run while this was
      # written: dependency-check needs the full NVD database, the locally cached copy was on an
      # older schema, and downloading it is a CI-shaped job rather than a laptop-shaped one. Flip
      # `continue-on-error` off once a run has been seen and any findings are either fixed or
      # suppressed with a reason.
      - name: Dependency vulnerabilities
        continue-on-error: true
        run: >-
          ./mvnw --batch-mode --no-transfer-progress
          org.owasp:dependency-check-maven:check
          -DfailBuildOnCVSS=7
          -DassemblyAnalyzerEnabled=false

      - name: Upload dependency report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: target/dependency-check-report.html
          retention-days: 7
          if-no-files-found: ignore

      # Credentials committed by mistake. .gitleaks.toml now allowlists the three known-dead keys —
      # the two burned production signing keys still in history, and the development key that is
      # committed on purpose — so a finding here should mean something new.
      #
      # STILL non-blocking, and this is the honest reason rather than a policy: gitleaks is not
      # installed on the machine this was written on, so the allowlist has never actually been
      # executed against this history. Flip `continue-on-error` off after one clean run. If that run
      # is not clean, extend the allowlist — under the rule stated at the top of .gitleaks.toml — do
      # not widen it to make the job green.
      - name: Secret scan
        continue-on-error: true
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: .gitleaks.toml
