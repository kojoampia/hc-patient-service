# Build, test and scan hc-patient-service.
#
# There was no CI in this repository at all before 2026-08-05, which is how a service with no
# object-level authorization, a committed JWT signing key and a forgeable audit trail stayed that way
# — the ArchUnit layer test, the JWT tests and the whole integration suite ran only when somebody
# remembered to run them locally.
#
# Scanning starts NON-BLOCKING (continue-on-error) on purpose. The dependency and secret scans will
# light up on day one — the committed keys are in git history and cannot be removed from it — and a
# job that is red on arrival is a job everybody learns to ignore. Triage the findings, then delete the
# continue-on-error lines so this actually gates. That second step is the point; without it this file
# is decoration.
name: Build and scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The pom targets 25 via maven.compiler.release and the Enforcer accepts [17,26). Temurin 25 is
      # what the production image runs (docker/api.Dockerfile), so it is what this builds on.
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: temurin
          cache: maven

      # The *IT suite starts MongoDB and Kafka through Testcontainers, so a working Docker daemon is
      # the only service dependency — `npm run services:up` is deliberately NOT needed here.
      - name: Build and test
        run: ./mvnw --batch-mode --no-transfer-progress verify

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            target/surefire-reports/
            target/failsafe-reports/
            target/site/jacoco-merged/
          retention-days: 7

  scan:
    name: Dependency and secret scan
    runs-on: ubuntu-latest
    env:
      # Mapped at job level so every step's `if:` can test it without repeating the mapping. It is
      # empty when the secret is not configured, and that emptiness is what gates the
      # dependency-check steps below.
      NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Secret scanning needs history, not just the tip: the point is to find what was committed
          # and later deleted, which is exactly the shape of this repo's JWT key.
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: temurin
          cache: maven

      # Known-vulnerable Java dependencies. Spring Boot 4.0.6 and Spring Cloud 2025.1.1 are current,
      # but nothing was checking, and "current today" has a short shelf life.
      #
      # SKIPPED UNLESS AN NVD API KEY IS CONFIGURED. The first CI run showed why: NVD now rejects
      # anonymous downloads outright — "Invalid API Key, length of 0" — so without a key this step
      # cannot succeed, ever. A step that always fails teaches everyone to ignore the job it lives
      # in, which costs more than the check is worth.
      #
      # To turn it on: request a key at https://nvd.nist.gov/developers/request-an-api-key and add
      # it as the repository secret NVD_API_KEY. The step then runs, and once its findings are
      # triaged `continue-on-error` can come off too.
      # The NVD dataset is on the order of a gigabyte and dependency-check downloads all of it on a
      # cold start — ten to thirty minutes, every run, which is enough to make people delete the job.
      # Restoring it turns that into an incremental update measured in seconds.
      #
      # Keyed by ISO week rather than by day, and that is a deliberate trade rather than laziness:
      # the cache is only a STARTING POINT, because every run still pulls incremental updates from
      # NVD using the key above. So a week-old cache does not mean week-old vulnerability data — it
      # means a shorter download. A daily key would save ~1 GB every day against a repository
      # allowance of 10 GB, and spend most of that allowance re-storing data that had not changed.
      #
      # restore-keys falls back to the most recent previous week when the exact key misses, so the
      # first run of a new week still starts from a warm dataset instead of from nothing.
      - name: Work out the NVD cache week
        if: env.NVD_API_KEY != ''
        id: nvd-cache
        run: echo "week=$(date -u +%G-W%V)" >> "$GITHUB_OUTPUT"

      - name: Restore the NVD dataset
        if: env.NVD_API_KEY != ''
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.dependency-check-data
          key: dependency-check-data-${{ runner.os }}-${{ steps.nvd-cache.outputs.week }}
          restore-keys: dependency-check-data-${{ runner.os }}-

      # dataDirectory is set explicitly rather than left to the plugin default
      # (~/.m2/repository/org/owasp/dependency-check-data). The default is a documented behaviour of
      # the plugin rather than a promise, and a cache whose path silently stops matching where the
      # data is written fails the worst way available: quietly, by restoring nothing and downloading
      # everything, while still reporting success.
      - name: Dependency vulnerabilities
        if: env.NVD_API_KEY != ''
        continue-on-error: true
        run: >-
          ./mvnw --batch-mode --no-transfer-progress
          org.owasp:dependency-check-maven:check
          -DfailBuildOnCVSS=7
          -DassemblyAnalyzerEnabled=false
          -DdataDirectory="$GITHUB_WORKSPACE/.dependency-check-data"
          -DnvdApiKey="$NVD_API_KEY"

      - name: Upload dependency report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: target/dependency-check-report.html
          retention-days: 7
          if-no-files-found: ignore

      # Credentials committed by mistake. .gitleaks.toml now allowlists the three known-dead keys —
      # the two burned production signing keys still in history, and the development key that is
      # committed on purpose — so a finding here should mean something new.
      #
      # BLOCKING as of the run on 6463ca4, which reported "no leaks found" with the allowlist loaded.
      # If this fails, extend .gitleaks.toml under the rule stated at the top of that file — do not
      # widen it to make the job green.
      #
      # Note what it actually scans: on a push the action examines the pushed commit range, not the
      # whole history. That is the right default for a gate, but it means the allowlist entries for
      # the burned keys only bite on a run that reaches back to them.
      - name: Secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: .gitleaks.toml
